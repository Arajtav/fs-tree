[package]
name = "fs_tree_shared"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[features]
metadata_timestamps = []
metadata_ownership = []
full_metadata = ["metadata_timestamps", "metadata_ownership"]
ignore_zero = []
count_files = []
sort = []

[dependencies]
rayon = "1.10.0"
