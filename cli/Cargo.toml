[package]
name = "fs_tree_cli"
edition = "2024"
publish = false

[dependencies]
clap = { version = "4.5.41", features = ["derive"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.141"
fs_tree_shared = { path = "../shared" }
